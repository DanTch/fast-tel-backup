# TeleZip Script Generator 🚀

[English](#english) | [فارسی](#persian)

---

<a name="persian"></a>
## 🇮🇷 راهنمای فارسی

**TeleZip Script Generator** یک ابزار تحت وب ساده و زیبا است که به شما کمک می‌کند اسکریپت‌های پایتون برای پشتیبان‌گیری امن (Backup) بسازید. این اسکریپت‌ها پوشه‌های سرور یا سیستم شما را فشرده کرده، رمزگذاری می‌کنند و مستقیماً به تلگرام شما می‌فرستند.

### ✨ ویژگی‌ها
- **رابط کاربری زیبا:** طراحی شده با React و Tailwind CSS.
- **تولید خودکار کد:** بدون نیاز به دانش برنامه‌نویسی کد پایتون دریافت کنید.
- **امنیت بالا:** استفاده از رمزنگاری AES-256 برای فایل‌های زیپ.
- **گزارش‌دهی هوشمند:** ارسال پیام وضعیت به تلگرام قبل از شروع و پاک کردن آن‌ها پس از اتمام موفقیت‌آمیز.
- **نوار پیشرفت:** نمایش درصد پیشرفت زیپ و آپلود در ترمینال.

### 🚀 نحوه استفاده (وب‌سایت)
1. اطلاعات ربات تلگرام (توکن و آیدی عددی) را وارد کنید.
2. مسیر پوشه‌ای که می‌خواهید بکاپ بگیرید را وارد کنید.
3. یک رمز عبور برای فایل زیپ تنظیم کنید.
4. کد تولید شده را کپی یا دانلود کنید.

### 🐍 نحوه اجرای اسکریپت پایتون
برای اجرای کدی که این سایت به شما می‌دهد، در سرور یا سیستم خود مراحل زیر را انجام دهید:

1. **نصب پیشنیازها:**
   ```bash
   pip install pyzipper requests tqdm requests-toolbelt
   ```

2. **ذخیره و اجرا:**
   فایل را با نام `backup.py` ذخیره کنید و دستور زیر را بزنید:
   ```bash
   python backup.py
   ```

### 🌐 اجرای پروژه روی گیت‌هاب (GitHub Pages)
این پروژه با Vite و React ساخته شده است. برای بالا آوردن سایت روی گیت‌هاب:

1. ریپازیتوری را فورک (Fork) کنید.
2. دستورات زیر را در سیستم خود بزنید:
   ```bash
   npm install
   npm run build
   ```
3. محتویات پوشه `dist` را در یک برنچ به نام `gh-pages` پوش کنید (یا از اکشن‌های گیت‌هاب استفاده کنید).
4. از تنظیمات ریپازیتوری، بخش Pages، برنچ مربوطه را انتخاب کنید.

---

<a name="english"></a>
## 🇺🇸 English Guide

**TeleZip Script Generator** is a sleek web utility that generates secure Python scripts for automated backups. The generated script compresses your directories, encrypts them with AES, and uploads them directly to your Telegram chat.

### ✨ Features
- **Modern UI:** Built with React & Tailwind CSS.
- **Auto Code Gen:** Get a ready-to-run Python script instantly.
- **High Security:** Uses AES-256 encryption for zip archives.
- **Smart Reporting:** Sends status updates to Telegram before starting and deletes them automatically upon success.
- **Progress Tracking:** Shows zip and upload progress bars in the terminal.

### 🚀 How to Use (Web App)
1. Enter your Telegram Bot Token and Chat ID.
2. Specify the source directory path (e.g., `~/sessions`).
3. Set a password for the encryption.
4. Copy or download the generated Python code.

### 🐍 How to Run the Script
To run the script generated by this tool on your server or local machine:

1. **Install Requirements:**
   ```bash
   pip install pyzipper requests tqdm requests-toolbelt
   ```

2. **Run:**
   Save the code as `backup.py` and run:
   ```bash
   python backup.py
   ```

### 🌐 Deploying to GitHub Pages
This project is built with Vite. To host it on GitHub Pages:

1. Fork this repository.
2. Install dependencies and build:
   ```bash
   npm install
   npm run build
   ```
3. Push the contents of the `dist` folder to a `gh-pages` branch.
4. Enable GitHub Pages from the repository settings.

---
*Made with ❤️ using React & Python*
